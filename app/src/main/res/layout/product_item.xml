<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="8dp">

    <ImageView
        android:id="@+id/product_image_item"
        android:layout_width="30dp"
        android:layout_height="30dp"

        android:contentDescription="img" />

    <TextView
        android:id="@+id/product_name_item"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="16dp"
        android:gravity="center_vertical"
        android:layout_alignTop="@id/product_image_item"
        android:layout_alignBottom="@id/product_image_item"
        android:layout_toEndOf="@+id/product_image_item"
        android:text="@string/product_name" />

    <TextView
        android:id="@+id/product_qtd_item"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentEnd="true"
        android:gravity="center_vertical"
        android:layout_alignTop="@id/product_image_item"
        android:layout_alignBottom="@id/product_image_item"
        android:layout_marginEnd="0dp"
        android:text="1" />

</RelativeLayout>